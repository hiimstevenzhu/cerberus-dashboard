import "./Titlebar.css";

// boilerplate code uses arrowed functions, but makes no difference
const Titlebar = () => {
  return (
    <nav className="Titlebar-maintext">
      <h1> Cerberus V1.0.0</h1>
    </nav>
  );
};

export default Titlebar;
